# Vercel Configuration for Static Portfolio

[build]
  command = "npm run build"
  functions = "functions"
  publish = "."

[context.production]

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
